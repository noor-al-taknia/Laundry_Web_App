import { sql } from "drizzle-orm";
import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const shops = sqliteTable("shops", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), shopName: text("shop_name").notNull(), shopNameAr: text("shop_name_ar").notNull().default(""), address: text("address").notNull().default(""), phone: text("phone").notNull().default(""), email: text("email").notNull().default(""), vatNumber: text("vat_number").notNull().default(""), commercialNumber: text("commercial_number").notNull().default(""), invoicePrefix: text("invoice_prefix").notNull().default("INV"), receiptFooter: text("receipt_footer").notNull().default(""), timezone: text("timezone").notNull().default("Asia/Riyadh"), currency: text("currency").notNull().default("SAR"), vatRateBasisPoints: integer("vat_rate_basis_points").notNull().default(1500), isActive: integer("is_active", { mode: "boolean" }).notNull().default(true), updatedBy: text("updated_by").notNull(), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_shops_tenant_name").on(table.tenantId, table.shopName), index("idx_shops_tenant_active").on(table.tenantId, table.isActive)]);
export const branches = sqliteTable("branches", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), shopId: text("shop_id").notNull(), name: text("name").notNull(), code: text("code").notNull(), address: text("address").notNull().default(""), phone: text("phone").notNull().default(""), isActive: integer("is_active", { mode: "boolean" }).notNull().default(true), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_branches_tenant_code").on(table.tenantId, table.code), index("idx_branches_shop").on(table.tenantId, table.shopId, table.isActive)]);
