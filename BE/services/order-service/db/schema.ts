import { sql } from "drizzle-orm";
import { check, index, integer, real, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const orderSequences = sqliteTable("order_sequences", { tenantId: text("tenant_id").notNull(), businessDate: text("business_date").notNull(), nextToken: integer("next_token").notNull().default(1), nextInvoice: integer("next_invoice").notNull().default(1), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_order_sequence_tenant_date").on(table.tenantId, table.businessDate)]);
export const orders = sqliteTable("orders", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), invoiceNumber: text("invoice_number").notNull(), tokenNumber: text("token_number").notNull(), customerId: text("customer_id"), customerName: text("customer_name").notNull(), customerPhone: text("customer_phone").notNull().default(""), customerEmail: text("customer_email").notNull().default(""), customerAddress: text("customer_address").notNull().default(""), createdBy: text("created_by").notNull(), assignedStaffId: text("assigned_staff_id").notNull(), orderDate: text("order_date").notNull(), supplyDate: text("supply_date").notNull(), paymentMethod: text("payment_method", { enum: ["cash", "card"] }).notNull(), cardAccount: text("card_account", { enum: ["stc", "anb"] }), cashReceived: real("cash_received").notNull().default(0), balanceSettledByStaff: integer("balance_settled_by_staff", { mode: "boolean" }).notNull().default(false), settledFromStaff: real("settled_from_staff").notNull().default(0), settledFromDrawer: real("settled_from_drawer").notNull().default(0), paymentStatus: text("payment_status", { enum: ["paid", "unpaid", "partial"] }).notNull(), orderStatus: text("order_status", { enum: ["active", "void"] }).notNull().default("active"), subtotal: real("subtotal").notNull(), discount: real("discount").notNull().default(0), vatAmount: real("vat_amount").notNull(), totalAmount: real("total_amount").notNull(), amountPaid: real("amount_paid").notNull().default(0), balance: real("balance").notNull(), notes: text("notes").notNull().default(""), version: integer("version").notNull().default(1), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_orders_tenant_invoice").on(table.tenantId, table.invoiceNumber), uniqueIndex("idx_orders_tenant_token").on(table.tenantId, table.tokenNumber), index("idx_orders_tenant_date").on(table.tenantId, table.orderDate), index("idx_orders_tenant_customer").on(table.tenantId, table.customerId), index("idx_orders_tenant_payment").on(table.tenantId, table.paymentStatus, table.paymentMethod), check("orders_payment_method_check", sql`${table.paymentMethod} IN ('cash','card')`), check("orders_card_account_check", sql`${table.cardAccount} IS NULL OR ${table.cardAccount} IN ('stc','anb')`), check("orders_status_check", sql`${table.paymentStatus} IN ('paid','unpaid','partial')`), check("orders_order_status_check", sql`${table.orderStatus} IN ('active','void')`), check("orders_amounts_check", sql`${table.subtotal} >= 0 AND ${table.discount} >= 0 AND ${table.vatAmount} >= 0 AND ${table.totalAmount} >= 0 AND ${table.amountPaid} >= 0 AND ${table.balance} >= 0 AND ${table.cashReceived} >= 0 AND ${table.settledFromStaff} >= 0 AND ${table.settledFromDrawer} >= 0`)]);
export const staffDebts = sqliteTable("staff_debts", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), staffUserId: text("staff_user_id").notNull(), orderId: text("order_id").notNull(), originalAmount: real("original_amount").notNull(), outstandingAmount: real("outstanding_amount").notNull(), status: text("status", { enum: ["open", "settled", "void"] }).notNull().default("open"), notes: text("notes").notNull().default(""), settledBy: text("settled_by"), settledAt: text("settled_at"), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_staff_debts_tenant_order").on(table.tenantId, table.orderId), index("idx_staff_debts_staff_status").on(table.tenantId, table.staffUserId, table.status), check("staff_debts_amount_check", sql`${table.originalAmount} >= 0 AND ${table.outstandingAmount} >= 0 AND ${table.outstandingAmount} <= ${table.originalAmount}`)]);
export const orderItems = sqliteTable("order_items", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), orderId: text("order_id").notNull(), serviceId: text("service_id").notNull(), categoryName: text("category_name").notNull(), serviceName: text("service_name").notNull(), unitPrice: real("unit_price").notNull(), quantity: real("quantity").notNull(), taxableAmount: real("taxable_amount").notNull(), vatAmount: real("vat_amount").notNull(), totalAmount: real("total_amount").notNull() }, (table) => [index("idx_order_items_order").on(table.tenantId, table.orderId), check("order_items_positive", sql`${table.unitPrice} > 0 AND ${table.quantity} > 0`)]);
export const orderEvents = sqliteTable("order_events", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), orderId: text("order_id").notNull(), actorUserId: text("actor_user_id").notNull(), eventType: text("event_type").notNull(), oldValue: text("old_value"), newValue: text("new_value"), occurredAt: text("occurred_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [index("idx_order_events_order").on(table.tenantId, table.orderId, table.occurredAt)]);
export const outboxEvents = sqliteTable("outbox_events", { id: text("id").primaryKey(), aggregateId: text("aggregate_id").notNull(), eventType: text("event_type").notNull(), payload: text("payload").notNull(), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), publishedAt: text("published_at") }, (table) => [index("idx_order_outbox_unpublished").on(table.publishedAt)]);
