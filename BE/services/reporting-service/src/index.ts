/* eslint-disable import/no-anonymous-default-export */
type Env = { DB: D1Database };
export default { async fetch(request: Request, env: Env) { const url = new URL(request.url); if (url.pathname === "/health") return Response.json({ service: "reporting-service", status: "ok" }); if (url.pathname === "/ready") { await env.DB.prepare("SELECT 1").first(); return Response.json({ ready: true }); } if (url.pathname === "/v1/dashboard" && request.method === "GET") { const date = url.searchParams.get("date"); const row = await env.DB.prepare("SELECT * FROM daily_metrics WHERE business_date = ? LIMIT 1").bind(date).first(); return Response.json({ data: row }); } return Response.json({ error: "Route not found" }, { status: 404 }); } };
