import { sql } from "drizzle-orm";
import { check, index, integer, real, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const expenses = sqliteTable("expenses", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), expenseNumber: text("expense_number").notNull(), expenseDate: text("expense_date").notNull(), category: text("category").notNull(), description: text("description").notNull(), amount: real("amount").notNull(), paymentMethod: text("payment_method", { enum: ["cash", "card", "bank"] }).notNull(), vendor: text("vendor").notNull().default(""), receiptReference: text("receipt_reference").notNull().default(""), notes: text("notes").notNull().default(""), status: text("status", { enum: ["posted", "void"] }).notNull().default("posted"), createdBy: text("created_by").notNull(), version: integer("version").notNull().default(1), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_expenses_tenant_number").on(table.tenantId, table.expenseNumber), index("idx_expenses_tenant_date_category").on(table.tenantId, table.expenseDate, table.category), check("expenses_amount_positive", sql`${table.amount} > 0`), check("expenses_method_check", sql`${table.paymentMethod} IN ('cash','card','bank')`), check("expenses_status_check", sql`${table.status} IN ('posted','void')`)]);
export const outboxEvents = sqliteTable("outbox_events", { id: text("id").primaryKey(), aggregateId: text("aggregate_id").notNull(), eventType: text("event_type").notNull(), payload: text("payload").notNull(), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), publishedAt: text("published_at") }, (table) => [index("idx_expense_outbox_unpublished").on(table.publishedAt)]);
