import { sql } from "drizzle-orm";
import { check, index, integer, real, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const categories = sqliteTable("categories", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), name: text("name").notNull(), color: text("color").notNull().default("#2563eb"), sortOrder: integer("sort_order").notNull().default(0), isActive: integer("is_active", { mode: "boolean" }).notNull().default(true), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_categories_tenant_name").on(table.tenantId, table.name), index("idx_categories_active_sort").on(table.tenantId, table.isActive, table.sortOrder)]);
export const serviceItems = sqliteTable("service_items", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), categoryId: text("category_id").notNull(), name: text("name").notNull(), nameAr: text("name_ar").notNull().default(""), sortOrder: integer("sort_order").notNull().default(0), isActive: integer("is_active", { mode: "boolean" }).notNull().default(true), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_items_category_name").on(table.tenantId, table.categoryId, table.name), index("idx_items_category_active").on(table.tenantId, table.categoryId, table.isActive, table.sortOrder)]);
export const itemPrices = sqliteTable("item_prices", { id: text("id").primaryKey(), tenantId: text("tenant_id").notNull(), itemId: text("item_id").notNull(), price: real("price").notNull(), effectiveFrom: text("effective_from").notNull(), effectiveTo: text("effective_to"), createdBy: text("created_by").notNull(), createdAt: text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [index("idx_prices_item_effective").on(table.tenantId, table.itemId, table.effectiveFrom, table.effectiveTo), check("item_prices_positive", sql`${table.price} > 0`)]);
