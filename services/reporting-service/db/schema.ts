import { sql } from "drizzle-orm";
import { index, integer, real, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const salesProjection = sqliteTable("sales_projection", { orderId: text("order_id").primaryKey(), tenantId: text("tenant_id").notNull(), tokenNumber: text("token_number").notNull(), invoiceNumber: text("invoice_number").notNull(), orderDate: text("order_date").notNull(), customerId: text("customer_id"), customerName: text("customer_name").notNull(), paymentMethod: text("payment_method").notNull(), paymentStatus: text("payment_status").notNull(), subtotal: real("subtotal").notNull(), vatAmount: real("vat_amount").notNull(), totalAmount: real("total_amount").notNull(), amountPaid: real("amount_paid").notNull(), balance: real("balance").notNull(), updatedAt: text("updated_at").notNull() }, (table) => [uniqueIndex("idx_sales_tenant_token").on(table.tenantId, table.tokenNumber), index("idx_sales_tenant_date_status").on(table.tenantId, table.orderDate, table.paymentStatus), index("idx_sales_tenant_customer").on(table.tenantId, table.customerId)]);
export const expenseProjection = sqliteTable("expense_projection", { expenseId: text("expense_id").primaryKey(), tenantId: text("tenant_id").notNull(), expenseDate: text("expense_date").notNull(), category: text("category").notNull(), amount: real("amount").notNull(), paymentMethod: text("payment_method").notNull(), status: text("status").notNull(), updatedAt: text("updated_at").notNull() }, (table) => [index("idx_expense_projection_date").on(table.tenantId, table.expenseDate, table.category)]);
export const dailyMetrics = sqliteTable("daily_metrics", { tenantId: text("tenant_id").notNull(), businessDate: text("business_date").notNull(), orderCount: integer("order_count").notNull().default(0), grossSales: real("gross_sales").notNull().default(0), collected: real("collected").notNull().default(0), outstanding: real("outstanding").notNull().default(0), cashCollected: real("cash_collected").notNull().default(0), cardCollected: real("card_collected").notNull().default(0), postedExpenses: real("posted_expenses").notNull().default(0), netOperating: real("net_operating").notNull().default(0), updatedAt: text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (table) => [uniqueIndex("idx_daily_metrics_tenant_date").on(table.tenantId, table.businessDate)]);
export const eventInbox = sqliteTable("event_inbox", { eventId: text("event_id").primaryKey(), sourceService: text("source_service").notNull(), eventType: text("event_type").notNull(), receivedAt: text("received_at").notNull().default(sql`CURRENT_TIMESTAMP`), processedAt: text("processed_at") }, (table) => [index("idx_event_inbox_unprocessed").on(table.processedAt)]);
