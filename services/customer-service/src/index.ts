/* eslint-disable import/no-anonymous-default-export */
type Env = { DB: D1Database };
export default { async fetch(request: Request, env: Env) { const url = new URL(request.url); if (url.pathname === "/health") return Response.json({ service: "customer-service", status: "ok" }); if (url.pathname === "/ready") { await env.DB.prepare("SELECT 1").first(); return Response.json({ ready: true }); } if (url.pathname === "/v1/customers" && request.method === "GET") { const search = `%${(url.searchParams.get("search") ?? "").slice(0, 100)}%`; const rows = await env.DB.prepare("SELECT * FROM customers WHERE is_active = 1 AND (name LIKE ? OR phone LIKE ? OR email LIKE ?) ORDER BY name LIMIT 50").bind(search, search, search).all(); return Response.json({ data: rows.results }); } return Response.json({ error: "Route not found" }, { status: 404 }); } };
