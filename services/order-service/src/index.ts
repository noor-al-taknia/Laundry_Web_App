/* eslint-disable import/no-anonymous-default-export */
type Env = { DB: D1Database };
export default { async fetch(request: Request, env: Env) { const url = new URL(request.url); if (url.pathname === "/health") return Response.json({ service: "order-service", status: "ok" }); if (url.pathname === "/ready") { await env.DB.prepare("SELECT 1").first(); return Response.json({ ready: true }); } if (url.pathname === "/v1/orders" && request.method === "GET") { const token = url.searchParams.get("token"); const rows = token ? await env.DB.prepare("SELECT * FROM orders WHERE token_number = ? LIMIT 1").bind(token).all() : await env.DB.prepare("SELECT * FROM orders ORDER BY created_at DESC LIMIT 50").all(); return Response.json({ data: rows.results }); } return Response.json({ error: "Route not found" }, { status: 404 }); } };
